# PWC CODE TEST
Test application for PWC <br/>
Please run by building the application using Maven first (mvn clean package). <br/>
Then run using (java -jar target/pwc-1.jar) <br/>
It uses Java Serialization instead of using databases.
Run the application and enter 1 to create a new address book. then create another one and compare them together.
